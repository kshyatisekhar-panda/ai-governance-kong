{
  "name": "apps",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p dashboard/shared/tsconfig.json",
    "start": "npm run build && npx serve . -l 3000 --cors"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
